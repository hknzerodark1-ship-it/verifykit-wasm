[package]
name = "verifykit-wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
verifykit-core = { path = "../core" }
wasm-bindgen = "0.2"
serde_json = "1.0"

[package.metadata.wasm-pack]
wasm-opt = false
